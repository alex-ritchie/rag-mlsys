name: gateway-baseline
target: rag-e2e                      # full RAG pipeline, end to end through the gateway
base_url: http://localhost:8000
serving_config: config/serving/vllm-qwen38-27b.yaml
concurrency: [1, 4, 8, 16, 32]
requests_per_level: 48               # per concurrency level; or use duration_s
warmup: 2
