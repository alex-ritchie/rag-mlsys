# Future (explicitly out of scope for v1 — design doc §10)

New ideas go here, not into the codebase.

- Multi-turn conversation memory / follow-up question rewriting.
- Jailbreak / prompt-injection eval harness (AI-safety extension of the judge harness).
- Helm/Kustomize packaging; multi-node k8s; replica-per-GPU vs tensor-parallel experiments on a 2-GPU box.
- Fine-tuning the reranker on golden-set relevance labels.
- Sparse (SPLADE / bge-m3 sparse) third retrieval leg in the RRF fusion.
- Ingest more sources (labs, kits) — requires a fresh licensing review.
- Per-section "explain like I'm a student" mode with reading-level control.
